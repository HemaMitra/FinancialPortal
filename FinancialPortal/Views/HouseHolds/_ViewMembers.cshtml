@model IEnumerable<FinancialPortal.Models.ApplicationUser>

@{
    ViewBag.Title = "Details";
}

    <div class="smart-widget">
    <div class="smart-widget-header">
        Household Members
        @*<span class="smart-widget-option">
            <a href="#" class="widget-collapse-option" data-toggle="collapse">
                <i class="fa fa-chevron-up"></i>
            </a>
         
        </span>*@
    </div>
    <div class="smart-widget-inner">
        <div class="smart-widget-body">
            <div>
                <table class="table">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(m => item.FirstName) @Html.DisplayFor(m => item.LastName)</td>
                        </tr>
                        
                    }
                    
                </table>
                
            </div> 
            @*<a href="@Url.Action("Details", "HouseHolds", new { id = @TempData["id"]})" class="btn btn-rounded text-white back-color">Back</a>*@     
        </div>
    </div>
    </div>

