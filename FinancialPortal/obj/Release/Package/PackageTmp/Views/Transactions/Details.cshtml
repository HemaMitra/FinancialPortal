@model FinancialPortal.Models.Transaction

@{
    ViewBag.Title = "Details";
}


<div class="smart-widget">
    <div class="smart-widget-header">
        Details for Transaction Id @Model.TransactionId
        <span class="smart-widget-option">
            <a href="#" class="widget-collapse-option" data-toggle="collapse">
                <i class="fa fa-chevron-up"></i>
            </a>
        </span>
    </div>
    <div class="smart-widget-inner">
        <div class="smart-widget-body">
            <div>
                <h4>Transaction Details</h4>
                <table class="table">
                    <tr class="strong">
                        <td>Account Name</td>
                        <td>Category</td>
                        <td>Is Withdrawl</td>
                        <td>Amount</td>
                        <td>Reconciliation Amount</td>
                        <td>Transaction Date</td>
                        <td>Description</td>

                    </tr>
                    <tr>
                        <td>@Html.DisplayFor(model => model.BankAccount.AccountName)</td>

                        <td>
                            @Html.DisplayFor(model => model.Category.CategoryName)
                        </td>

                        <td>
                            @Html.DisplayFor(model => model.IsWithdrawl)
                        </td>

                        <td>
                            @Html.DisplayFor(model => model.TransactionAmount)
                        </td>

                        <td>
                            @Html.DisplayFor(model => model.ReconciliationAmount)
                        </td>

                        <td>
                            @Model.TransactionDate.ToString("MMM dd, yyyy")
                        </td>

                        <td>
                            @Html.DisplayFor(model => model.TransactionDescription)
                        </td>
                    </tr>
                </table>
            </div>
            <p>
                <a href="@Url.Action("Edit", "Transactions", new { id = Model.TransactionId })" class="btn btn-rounded text-white back-color">Edit</a>
                <a href="@Url.Action("Index", "Transactions", new { BankAccountId = Model.BankAccountId })" class="btn btn-rounded text-white back-color">Back</a>
            </p>
        </div>
    </div>
</div>







